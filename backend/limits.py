# ── Application limits ────────────────────────────────────────────────────────
# Change values here — they propagate to backend enforcement and are exposed
# to the frontend via the GET /api/preferences/ response envelope.

MAX_ACTIVE_PREFERENCES_PER_USER = 3
